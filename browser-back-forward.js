/* Browser Back/Forward Challenge */

// Initialize two stacks: one for back navigation, one for forward navigation
// Variable to store the current page

// Create function to visit page
  // if current page is not null
    // Push current page onto the back stack
  // clear forward stack since new page was visited
  // Set the new page as the current page

// Create function to go back to previous page
  // if stack is empty return
  // push the current page onto the forward stack
  // pop the most recent page from the back stack and set it as the current page

// Create function to go forward
  // if forward stack is empty return
  // push current page onto the back stack
  // pop recent page from forward stack and set it as the current page

